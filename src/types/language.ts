/**
 * Language code type for internal language detection and TTS logic.
 * These codes are decoupled from display text (which goes through t()).
 */
export type LanguageCode = "zh" | "en" | "ja" | "other";
