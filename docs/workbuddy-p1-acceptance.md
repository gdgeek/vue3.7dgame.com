# WorkBuddy P1 试用验收

状态：部分完成。2026-09-18 续跑已恢复本地 API 和配对，实体脚本读取、目录查询通过；暂存被未保存基线拦截，随后 WorkBuddy 积分再次耗尽，尚未完成实体脚本 MCP 写入。另补足连接中退出及换账号不继承连接的浏览器/独立 MCP 证据。承接 [P0 验收](./webmcp-dev-acceptance.md)，仅使用隔离本地环境，不发布生产、不修改第三方桥接、不修改 Editor/Blockly 插件。自动化、实际客户端、实际数据库证据分别记录。

## 本轮对象

- 数据库：`xrugc_webmcp_p0_20260916`，真实 P1 API `127.0.0.1:8191`。
- 当前账号：专用普通用户 `896`。
- 新实体：`1091`，节点 `e58d3ec7-c1b7-49f9-8df3-8df0b3db9978`。
- 主测试场景：`666`，实例 `4f06f25f-e324-4ae4-87ff-f583e0e95a01`。
- 上下文切换场景：`667`，实例 `bc11f53b-d715-4671-8015-aa5b0a14f2e3`。
- P0 对象 `665/1090` 保留。

## 执行清单

- [x] 场景重复 complete、取消后确认：实际 WorkBuddy 同一操作 ID，取消后点击旧确认不落库。
- [x] 确认前断线及提交后响应丢失：已实测不重放；按原操作 ID 查到服务器回执。
- [ ] 场景、实体、场景脚本、实体脚本的代表性读写和刷新读回。
- [ ] 四页人工保存与两个入口交叉调用；区分原生 API 实测与模拟注册器。
- [x] 版本冲突保留本地修改，服务器返回 409，新版本不被覆盖。客户端总结的独立问题见下文。
- [ ] 实际同源第二标签锁互斥与首连接释放。
- [ ] 切场景、脚本抽屉开关、iframe 重建：旧上下文与旧草稿失效。
- [ ] 退出、登录失效、换账号撤销旧会话；正常令牌刷新保留连接。
- [x] 参数 schema、错误、partial/unknown/awaiting_confirmation 及生命周期自动化回归。
- [x] 类型检查、生产构建、受影响文件 lint、固定版本桥接回归。
- [ ] 连接/排错说明、证据和已知限制收口。

每项只有实际证据具备后才勾选；原生浏览器能力、已部署开发环境等缺失项应明确记录，不用模拟结果替代。

## 当前已取得的真实证据（2026-09-17）

- C1：`14761fc9-6987-4f92-8b3b-13bcb8c16c70` 两次 complete 均返回同 ID 的 awaiting_confirmation（295/123 ms），取消后点击页面旧确认，数据库无此操作、位置仍 0。
- C2：`8dc5f96a-4776-4dea-b115-161ff83354fe` 确认前手动断开桥接，再点击页面确认，位置 0.2 落库一次。重新配对后查询 completed；再次 complete 仍同 ID，取消已完成操作返回 not_cancellable。
- S1：`f7a325ba-d753-4898-a616-168cdfdd4209` 场景脚本保存两个无副作用积木，生成 JS/Lua。实际关闭/重开 iframe 后读回相同积木、ID 和代码；切换和加载阶段旧 contextToken 被拒绝。
- L1：第二同源标签尝试连接，显示“已有其他标签页连接 AI 工具，请先在该页面断开。”第一标签保持可用。页面刷新后令牌输入为空，需重新配对。
- 人工场景编辑 x=0.4 已通过正常编辑流程保存，操作 `7f5cbfdd-9af5-4bc7-a3f1-69474d65dbc7`。编辑器自动保存开启，不能把随后点击“保存”出现的“项目没有改变”解释为未保存。

## 验收中发现并修复

场景脚本保存推进服务端 revision，但 VerseScriptDrawer 丢弃 saved 事件 payload，主场景仅失效预览缓存，下一次场景保存使用旧 revision 而遭到 409。现增加有场景 ID/前序版本校验的脚本回执传递，仅同步 revision 和脚本，不覆盖 3D 编辑或清除 dirty。

第一次 C3 故障测试 `537669e2-2848-4c07-ab8b-590dab52027f` 丢失的是 **409 响应**，不是成功保存响应。实际工具查询为 partial / editorApplied=true / persistence=unverified；数据库没有该操作。该次不能算成功提交后的响应丢失恢复验收，必须在修复版重测。

修复版 UI `v2026.09.17-0111`，本机验收构建 `/private/tmp/xrugc-webmcp-p1-fixed-build`；Blockly 使用本机 3003（原 3000 被其他 Docker 服务占用），Editor 3002、前端 3102、隔离 API 8191。

## 修复后复测与客户端限制

- C3 复测：`5c33525d-0012-4728-9a3d-5f8e7567f249`，人工脚本 FALSE→TRUE 保存后，同页主场景 x=0.4→0.5 写入返回 HTTP 200，证明版本同步修复有效。代理在收到完整上游响应后主动关闭下游连接。WorkBuddy 查询得到 partial（保持原义）+ 匹配的 completed writeReceipt + server_acknowledged，未重试写入。独立数据库只有一条该操作回执。
- 真冲突：旧标签页保持旧 revision，人工 x=0.6 保存于 UTC 17:16:27 返回 409；页面保留 0.6，数据库保持新版本。关闭旧标签前已记录 UI 和数据库证据。
- 新增连接/登录事件集成测试 5 项；脚本回执转发与 revision 保护测试 7 项。最终相关回归 41 文件 / 348 项通过（包含 meta 编辑页、脚本页和 WebMCP）。修复后类型检查、默认关闭的生产构建、受影响源码 lint 通过。固定上游 tarball 完整性验证与真实协议 smoke 通过。
- WorkBuddy 长任务曾停留在上下文压缩；正常重启并新建简短任务后恢复。新任务首次未正确发现站点工具，调用了一次 define-mcp-tool；纠正并重新配对后完成 C3。客户端还曾把 awaiting_confirmation 总结为“已保存”，属于错误叙述，原始工具/数据库并未误报成功。不能据此宣称客户端完全可靠。
- 实体 1091 页面已连通并显示 37 工具；当前 WorkBuddy 输入反复延迟，已请求用户协助发送实体测试指令，实体/实体脚本真实读写尚未完成。

证据：[第一段真实调用](./evidence/workbuddy-p1-client-20260917.json)、[后续真实调用](./evidence/workbuddy-p1-continuation-20260917.json)、[独立数据库与响应故障](./evidence/workbuddy-p1-server-20260917.json)。回执含业务 partial 状态时不要抹去它，也不要把编辑器当前内存读回冒充刷新后的持久化读回；本轮落库证据来自独立数据库。

当前环境没有可调用的原生浏览器 WebMCP 客户端接口，跨入口仅有自动化注册器/共同草稿验证；原生真实客户端与已部署开发环境未验收。豆包沿用 P0“已发现官方 STDIO 入口、业务未验收”的状态。本轮不发布生产，未修改 Editor/Blockly、上游桥接或后端业务接口。

## 当前阻塞与续跑位置

2026-09-17 01:38 左右，桌面工具明确报告 Mac 已锁屏且自动解锁失败。已请求用户手动解锁；此前原生输入出现延迟，不能在锁屏期间继续操作或推定发送成功。当前 Chrome 保留实体 1091，桥接连接时可发现 37 工具；WorkBuddy 当前任务为 “Adjust scene666 stage x value via MCP”（`cad64bda-6c46-4d7b-991b-8d355a768b73`）。解锁后先检查输入草稿是否有重复/残留，确认只有一条指令再发送，不能盲点发送。

续跑顺序：实体节点 x=0→0.1 确认保存和读回；实体脚本读写及 iframe 重建；四页补足刷新读回和人工保存；测试第二标签在首连接释放后获取连接；测试旧草稿跨对象/会话拒绝；实际退出、失效及换账号生命周期。原生真实客户端能力与部署开发环境仍须单独具备条件后验收。

另已在独立 API 使用新普通测试用户 897 验证：读取用户 896 的 C3 回执返回 403，见 [账号隔离证据](./evidence/workbuddy-p1-auth-isolation-20260917.json)。这不替代 WorkBuddy/浏览器的换账号生命周期实测。用户 897 仅存在于本地隔离数据库，未修改真实用户权限。

P1 尚未全部通过，不能标记全面 WorkBuddy 兼容或作为生产发布依据。


## 解锁后续跑（2026-09-17 08:00 左右）

- 用户已解锁；WorkBuddy 原生输入仍不响应。正常退出并重新启动后，历史任务草稿仍包含此前重复文本，未盲点发送；已请求用户手动清空并发送仅生成配对令牌的指令。没有把输入动作成功当成客户端执行成功。
- 发现本机前端 3102、Editor 3002、Blockly 3003 服务已停止；恢复同一验收构建和本地插件服务，API 8191 正常。首次退出因前端静态资源无法加载未完成，不计通过。
- 服务恢复后实际退出用户 896：API `POST /v1/auth/logout` 返回 200（UTC 23:59:14），页面回到未登录首页。登录用户 897 后，首页显示 `WorkBuddy P1 Second Account`；连接弹窗显示“未连接 · 当前工具：0”，令牌为空。该结果证明正常浏览器退出/换账号界面可用，但没有覆盖连接中的撤销竞态、旧 contextToken 拒绝或令牌刷新；完整生命周期项仍不勾选。

已恢复用户 896，并保留实体 1091 续跑页；独立数据库对象与 C3 成功响应丢失后的快照完全一致。见 [解锁后续跑记录](./evidence/workbuddy-p1-resume-20260917.json)。当前需用户在 WorkBuddy 手动清空重复草稿并发送仅生成令牌的请求；尚未发送实体修改。


## 新令牌配对与工具发现阻塞（2026-09-17 08:06–08:17）

- 使用用户提供的 08:05 新令牌，实体 1091 页面确认“已连接 · 当前工具：37”。令牌未写入证据文件。
- 原生输入通过 `typeText` 恢复，已实际发送只读实体指令。原任务重新搜索多次，以及全新短任务 `Accept P1 XRUGC entity via MCP`，均没有发现网站两个固定入口。新任务曾自行获取额外令牌并尝试不存在的连接器根工具，但没有成功执行网站业务工具。
- 独立固定版本 stdio 客户端 `tools/list` 于 UTC 00:11:41 返回 `127_0_0_1_3102-xrugc_webmcp_list_tools` 和 `127_0_0_1_3102-xrugc_webmcp_call_tool`。这证明桥接当前确有注册，不能将 WorkBuddy 搜索不到归因于网站没有工具；也不替代 WorkBuddy 验收。见 [工具发现证据](./evidence/workbuddy-p1-discovery-20260917.json)。
- WorkBuddy 新任务错误建议必须调用 define-mcp-tool 才能读取，实际不需要定义第二套工具；本轮没有执行该建议。新短任务也未恢复，不能宣称“新建任务一定解决”。
- 已请求用户在连接器管理中将 `xrugc-webmcp-dev` 关闭再开启；原生管理弹窗不能可靠操作，尚未验证重连能解决。按原计划的可靠性门槛，修复或明确官方配置可恢复前，不继续扩展兼容范围、不维护桥接分支。
- 独立普通编辑回归：实体 1091 节点 x=0→0.05，保存回执 `16fc7a7b-82cc-46e4-b895-6f4d2f2dcaed` completed；另一新加载页面读回 x=0.050，y/z=0、旋转=0、缩放=1。见 [人工实体保存和读回](./evidence/workbuddy-p1-manual-entity-20260917.json)。这是人工流程，不能勾选 WorkBuddy 实体写入。后续 E1 应从实时值 0.05 暂存到 0.1，不再假设初值为 0。


## 实际关闭再开启连接器复测（2026-09-17 09:07–09:12）

已通过 WorkBuddy 原生管理弹窗完成 `xrugc-webmcp-dev` 关闭（灰色开关、2 enabled）再开启（绿色开关、3 enabled），并等待状态点恢复绿色。页面仍保持“已连接 · 当前工具：37”。用户无需再重复此操作。

随后在 `Accept P1 XRUGC entity via MCP` 实际发送只读复测指令。原始 ToolSearch 结果仍只包含桥接基础工具，没有网站两个入口；实体读取未执行成功。独立固定版本 stdio `tools/list` 再次确认网站两个入口仍存在。关闭再开启连接器**没有解决本次工具发现问题**，不要将其写成已验证有效的恢复方法。WorkBuddy 的总结仍错误声称必须 define-mcp-tool；本轮未执行该建议。

证据：[连接器实际重启及复测](./evidence/workbuddy-p1-connector-restart-20260917.json)。按计划的可靠性门槛，P1 继续保留“未全部通过”；不维护上游桥接分支，不新增第二套业务入口，不发布生产。


### 09:43 再次重试

按用户要求，在当前连接保持的情况下再次发送只读工具发现/实体读取指令。WorkBuddy 25 秒后结束；原始搜索仍没有网站两个固定入口，实体读取未执行，没有生成令牌或执行写入。页面仍显示已连接、37 工具。不能采用客户端“网站没有注册工具”的解释，之前独立 stdio 检查已证实注册存在。见 [09:43 重试证据](./evidence/workbuddy-p1-retry-0943-20260917.json)。


## 完整重启、新配对恢复及积分阻塞（2026-09-17 09:54–10:04）

用户关闭 WorkBuddy 后，重新启动 5.5.2，保留原网页配对的第一次搜索仍只有基础工具。随后让实际客户端生成新令牌，网站先断开旧会话再使用新令牌连接，恢复“已连接 · 37 工具”。原 WorkBuddy 任务随后成功执行 list、实体工作区读取和节点树读取。仅开关连接器/完整重启并保留旧配对均未恢复；**完整重启后新令牌重新配对**在本轮实际恢复。不能推定以后每次都能恢复，也未改上游实现。

E1 已完成：读取节点 x=0.05，暂存至 0.1，complete 在 214 ms 返回 awaiting_confirmation 和操作 `a97970e0-2cdb-4b1b-b7be-91670828bcbf`；页面确认后，实际客户端查询 completed / server_acknowledged，节点 dirty=false。独立数据库只有一条对应 completed 回执，新加载的独立页面读回 x=0.100，y/z=0、旋转=0、缩放=1。没有重复写入。

实体脚本阶段：WorkBuddy 成功打开脚本抽屉，重新 list 时处于载入阶段，只返回 3 个工作区/打开/关闭工具。页面随后载入正常，但客户端提示 **“You’ve used all your available credits. Upgrade or invite friends to continue.”**，无法继续；没有暂存或提交脚本修改。本轮未购买积分或升级账号。

证据：[实体实际调用](./evidence/workbuddy-p1-entity-client-20260917.json)、[重启恢复、数据库回执与新页面读回](./evidence/workbuddy-p1-restart-recovery-20260917.json)。原有四页/登录生命周期/原生真实客户端等未完成项仍保持未通过。剩余实体脚本、上下文与账号生命周期真实测试须在 WorkBuddy 额度恢复后继续；不要为完成验收改用模拟调用冒充真实客户端。

## 再次续跑（2026-09-18 01:55–02:15）

- WorkBuddy 5.5.2 启动后账户菜单显示 30 积分，成功生成新令牌。Chrome 原保留标签的控制连接曾超时，原生激活该标签后恢复。临时 API 源码缺少 `advanced/api/web/index.php`，登录、刷新及对象读取返回 404；从本地 Git 的同一验收提交恢复临时源码后只读部署接口及登录返回 200。未改变后端业务代码或数据库配置。恢复前后 P0/P1 对象数据、脚本哈希及既有回执一致，不能把端口监听当成健康检查通过。
- 重新登录 896、配对后实体页显示 37 工具；重新打开实体脚本抽屉并等待就绪，实际 WorkBuddy `list_tools` 返回 14 工具。`get_meta_script` 返回 0 积木、有效空工作区 `{"backpack":[]}`；两次积木目录查询成功找到 `controls_if` 和 `logic_boolean`。
- 实际 `stage_script_block_batch` 尝试创建 FALSE 条件和空执行体，在 130 ms 返回 `tool_error`：当前脚本存在未保存修改。没有产生草稿/操作 ID，没有调用 complete，也没有 MCP 写入。空存储 `{}` 与编辑器序列化 `{"backpack":[]}` 的基线差异是本轮观察到的前置障碍，尚未修改插件或业务实现。
- 随后通过普通页面“保存脚本”保存空工作区基线，HTTP 200，操作 `e2c27973-97a5-4126-8666-7377d00ad136`、action `save_code`、completed；JS/Lua 仍空，实体 3D 数据哈希不变。明确归类为人工基线保存，不能计作 WorkBuddy 脚本写入验收。
- WorkBuddy 本轮结束再次显示积分耗尽，未购买或升级。余额恢复并不代表足以完成全部 P1；长任务重查完整 schema、目录及多轮推理仍有明显消耗。后续可使用简短新任务减少历史负担，但尚无本轮成本对比验证。
- 连接中退出检查：退出前页面“已连接 · 37 工具”，HTTP logout 于 UTC 18:09:54 返回 200。独立固定版本 stdio 的 `tools/list` 仍显示两个固定入口，但对旧 list 入口实际调用返回 `isError=true / Tool not found`，没有访问新页面。登录测试账号 897 后 UI 显示新身份、“未连接 · 0 工具”、令牌为空。该检查来自真实浏览器和独立诊断客户端，不冒充 WorkBuddy 退出后调用；仍未覆盖旧 contextToken 重放、正常 JWT 刷新、登录失效事件和完整标签锁交接。

证据：[本轮 WorkBuddy 原始调用](./evidence/workbuddy-p1-entity-script-client-20260918.json)、[环境恢复、人工基线与连接退出记录](./evidence/workbuddy-p1-resume-20260918.json)。续跑从实体 1091 已保存空脚本基线开始，先重新配对并查询当前 schema，再暂存、验证、complete 一次、页面确认、查询回执和重建 iframe 读回。P1 仍未全部通过。
