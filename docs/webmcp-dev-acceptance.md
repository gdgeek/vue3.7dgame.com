# webmcp.dev 兼容验收记录

日期：2026-09-16 至 2026-09-17（Asia/Shanghai）。状态：**P0 隔离本地场景闭环通过：读取、暂存、页面确认、保存回执及刷新读回均已核验；完整试用阶段的其他用例仍待验收**。

本记录区分自动化、真实协议与真实业务证据。测试注册器、Node WebSocket 或仅能发现工具，均不能替代 WorkBuddy 自主完成编辑保存。

## 版本与环境

| 对象             | 当前记录                                                                                                               |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 平台前端         | `web` HEAD `c9a5fbd0` 加本次未提交改动；实际界面 `v2026.09.16-2309`，构建时间 2026-09-16 23:11:01                      |
| 后端源码         | 本轮运行隔离 P1 API `966514522fc1236da24c721a40c98dd216da32f6`；未使用缺少 P1 的旧 `87313dc3` 本地服务作为业务验收环境 |
| 上游桥接         | npm `@jason.today/webmcp@0.1.13`，已核验 tarball integrity；发布 bundle 的 MCP 元信息为 `0.1.12`                       |
| 客户端           | WorkBuddy 5.5.2，23:08 注册令牌调用成功；23:13 与 Chrome 独立协议页配对并自主完成两个固定入口调用                      |
| 浏览器           | Chrome 152.0.7977.83；独立协议页使用真实 navigator.locks                                                               |
| Editor           | `b6b5027` 加既有未提交改动；本地 TypeScript 编译成功，`http://127.0.0.1:3002/`                                         |
| 业务后端         | `http://127.0.0.1:8191`，专用 API/Redis 容器与 `xrugc_webmcp_p0_20260916` 数据库；环境说明见下文链接                   |
| 测试对象/操作 ID | scene `665`、meta `1090`、测试 user `896`；operation `357200bb-58f6-41cc-86b1-81083d06f980`                            |

## 已取得的证据

| 层级                              | 结果                                                            | 边界                                                                                                                                                                                                                 |
| --------------------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| transport 单元测试                | 27 项通过，两个新增文件 ESLint 通过                             | 覆盖端点校验、真实 wire 格式、握手/超时、锁互斥与释放、旧会话隔离、重复帧、结果与异常                                                                                                                                |
| 相关自动化回归                    | 最终一轮 38 个文件、288 项通过；受影响文件 ESLint 通过          | 自动化不替代真实客户端或运行后端验收                                                                                                                                                                                 |
| 生产构建与类型检查                | `pnpm run build`（含 `vue-tsc --noEmit`）退出码 0               | 存在既有组件命名及打包大小警告；未生产发布                                                                                                                                                                           |
| 功能开关开启的开发构建            | `VITE_WEBMCP_DEV_ENABLED=true` 的 development Vite 构建退出码 0 | 输出 `/tmp/xrugc-webmcp-enabled-build`，确认独立 `WebMcpDevDialog` 异步 chunk；默认关闭的生产构建也通过                                                                                                              |
| 原始发布包协议冒烟                | 通过                                                            | 真实 stdio/WS 配对、工具命名前缀、工具变化通知、MCP 结果/错误、多客户端、上游首标签路由及缓存行为                                                                                                                    |
| 成品 transport 与原始发布包集成   | 通过                                                            | 实际适配器的连接、发现、调用、业务错误、内存会话重连与清理；Web Locks 使用测试替身                                                                                                                                   |
| WorkBuddy 配置 UI                 | 固定版本配置已保存，已 Trust，连接器绿点启动                    | `Experts · Skills · Connectors → Connectors → Custom connectors → Configure MCP`；UI 显示用户目录 `.workbuddy-ai/mcp.json`；绿点不代表业务工具可调用                                                                 |
| WorkBuddy 客户端 → stdio 桥接预检 | 通过：23:08 第二次实际调用成功生成注册令牌                      | 真实工具名 `mcp__xrugc-webmcp-dev___webmcp_get-token`；首次查询时启动未完成，stdio 启动共耗时 24.377 秒。未记录令牌，网页配对及两个固定入口已在下述只读协议页验证；本行仅记录桥接预检，真实业务结果见下节            |
| WorkBuddy → Chrome 两个固定入口   | 23:13 通过：配对 connected，自主先 list 再按 schema call        | `http://127.0.0.1:3101`，源码 `/tmp/xrugc-webmcp-client-probe`；实际 registry/facade/transport、真实 navigator.locks，仅 1 个只读 echo fixture，无业务或认证                                                         |
| Chrome 同源双标签连接             | 通过：第二标签被拒绝，首连接未受影响                            | 第二标签实际提示“已有其他标签页连接 AI 工具，请先在该页面断开。”；这仅验证独立协议页的真实 Web Locks                                                                                                                 |
| WorkBuddy 与真实编辑页写入        | P0 场景修改、确认、保存及刷新读回通过                           | 实例 x 从 0 改为 0.25，数据库仅 1 次写入。刷新后重新发现 28 个工具，读回位置、回执与版本一致；ready=true、dirty=false、published=false                                                                               |
| 豆包桌面版                        | 已发现官方本地 STDIO 入口，业务兼容未验证                       | 当前 `/Applications/Doubao.app` 2.28.13 实际 UI 的“技能·连接器·伙伴 → 新建 → 新建自定义连接器”可选 HTTP/STDIO；选择 STDIO 后出现服务器名称、命令、参数、环境变量。探测已结束，未保存任何新连接器、未配对、未执行工具 |

协议证据可用 `node scripts/webmcp-dev-protocol-smoke.mjs /absolute/path/to/verified/package` 重现。完整启用与固定版本配置见 [接入说明](./webmcp-dev.md)。构建、类型检查、lint、自动化及协议测试通过；业务结论以本次隔离环境的独立证据为准，不能据此推及线上开发或生产环境。

独立协议页的实际工具结果如下，Chrome 调用计数为 1。该结果仅证明真实客户端可使用两个固定入口，不包含场景修改、人工确认、保存、登录生命周期或业务后端：

```json
{ "probe": "protocol-only", "message": "workbuddy-chrome-probe-20260916" }
```

23:14 在协议页替换工具上下文后，WorkBuddy 使用旧 `contextToken` 调用收到 `stale_context`，随后自主重新查询并返回 `{"probe":"protocol-only","message":"fresh-context-ok"}`。Chrome 最终调用计数为 2（首次成功和新上下文成功），证明旧请求没有执行。此结果覆盖实际客户端的上下文校验，但不能替代真实切场景或 iframe 生命周期验收。

该独立协议页测试结束后已主动断开页面、关闭两个临时标签页并停止 `127.0.0.1:3101` 探针服务；WorkBuddy 固定版本连接器配置保留。该协议探测未修改业务数据。随后进行的隔离本地真实场景测试见下节，未发布生产。

## 隔离本地真实场景验证（2026-09-17）

本轮采用 [P0 隔离本地环境](./workbuddy-p0-local-environment.md) 的真实登录、Editor 与 P1 API。结构化结果见 [本轮非秘密证据](./evidence/workbuddy-p0-20260917.json)。旧 `localhost:8091` 缺少 P1、`bujiaban_test` 不存在是建立隔离环境的历史原因，已不再作为本轮业务测试的当前阻碍。

- 对象为 scene `665`、meta `1090`、测试 user `896`，实例 `d17d2e3a-db87-4f52-b9a3-734b8475aa09`。WorkBuddy 将实例位置 x 从 `0` 改为 `0.25`，其他 transform 字段及 meta 保持不变。
- WorkBuddy 完成 stage 后，调用 `xrugc_complete_scene_module_transform`。客户端日志记录起止为 `2026-09-16T16:04:24.162Z` 至 `16:04:24.493Z`，331 ms 返回 `awaiting_confirmation` 和 operation `357200bb-58f6-41cc-86b1-81083d06f980`，未等候页面确认才返回。
- 可见确认框至少保留 47 秒；Codex 按用户对本次测试的授权，在页面点击确认。数据库核对同一 operation 仅有 1 次写入，场景状态仍为 `notpublished`、snapshot 为 `0`。这一计数证明本次调用的结果，不代表已经执行重复提交或跨入口幂等故障测试。
- 页面完全刷新后重新配对，WorkBuddy 于 `16:22:14Z` 重新发现 28 个工具，随后使用新的上下文完成四次只读调用：操作回执、场景实例、工作区和编辑器状态。位置为 `(0.25, 0, 0)`，旋转 `(0, 0, 0)`、缩放 `(1, 1, 1)`；`ready=true`、`dirty=false`、`published=false`。原操作为 `completed` / `server_acknowledged`，回执与当前场景版本均为 `sha256:0cdce0852729e24ddf472b274b1c6af8d200c7fdcce89c086b35b692947fd54a`。网页属性面板也显示相同位置。
- 最终读回回合仅有客户端 ToolSearch 和 MCP 查询，没有 shell、文件或业务写入；实际四次业务调用按顺序完成。工具结果全部返回后，本次 UI 验收操作结束了回复生成，客户端显示 Cancelled；该回合的验收依据是原始工具返回，不是最终总结文案或客户端任务状态。该结果不能扩大为四类编辑页、冲突处理或所有异常恢复均已通过。

客户端与桥接限制也一并保留：

- 回执复核回合曾明确限制为 MCP-only，但同一 WorkBuddy 任务在 `16:07:09.579Z` 调用 Bash 列目录、`16:07:26.327Z` 调用 Bash 创建目录，并在 `16:07:38.515Z` 通过 Write 写入任务目录的 `.workbuddy-ai/memory/2026-09-17.md`。客户端结果均为 completed，文件存在且与请求写入内容一致。只读审计未发现 JWT 形态值、Base64 JSON 注册令牌或凭据赋值字段；未在证据中复制文件内容或秘密。Codex 随后停止偏离回合并重新约束。此问题属于客户端遵循指令的限制，与已核验的网页保存结果分别记录。
- 刷新恢复配对时第一个新令牌被拒绝，第二次获取后恢复连接。上游 `0.1.13` 的整文件令牌表覆盖存在并发竞态的可能，但**本次拒绝的唯一原因尚未证实**，不能直接归因为该竞态。没有记录实际令牌，也未修改上游桥接分支。

## 真实客户端待验收清单

每项记录浏览器和客户端版本、前后端实际运行版本、独立测试对象 ID、步骤、操作 ID、预期与实际结果。保存结果必须从实际后端或刷新后的编辑器核对。

- [x] WorkBuddy 自主使用“查询当前工具 → 读取场景 → 暂存位置修改 → 网页人工确认 → 查询保存回执”，刷新后结果一致；不依赖反复人工教导固定入口。
- [x] 页面确认至少等待 47 秒，初始 complete 在 331 ms 内返回操作 ID 和 awaiting_confirmation；本次数据库写入已核对。
- [x] WorkBuddy 完成最终回执及刷新后的场景读回，核对同一 operation 与实际保存结果（依据实际工具返回）。
- [ ] 同一草稿重复提交及原生/兼容入口交叉提交最多产生一次保存；取消确认不产生写入。
- [ ] 确认前及提交后的断线分别测试；重连不自动重放，已提交保存不被误报为取消，原操作回执仍可核对。
- [ ] HTTP 409 保留本地修改并显示冲突，不静默覆盖、不返回保存成功。
- [ ] Chrome 中第二个同源标签页被拒绝连接，首标签断开后第二个可连接；刷新、退出登录、账号切换、正常 token 刷新符合约定。
- [ ] 切场景、开关脚本抽屉、重建 iframe 后旧 contextToken 被拒绝，当前工具集与页面一致。
- [ ] 第二阶段补齐实体、场景、实体脚本、场景脚本的代表性读写，以及原生入口和人工编辑回归。

本轮可标记为“WorkBuddy P0 本地场景流程已验证”，适合继续开发环境试用。尚未在已部署开发环境完成四类编辑页与全部异常用例，不标记全面兼容、不启用生产。后续若必须维护桥接分支或固定入口无法稳定使用，应停止扩大适配范围并记录结论。
